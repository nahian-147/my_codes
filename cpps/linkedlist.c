#include<stdio.h>
#include<stdlib.h>

struct node{
    struct *left = NULL;
    struct *right = NULL; 
    double value;
};

void insert(struct node *P,double val){
    struct node* tmp = (struct node*)malloc(sizeof(*P));
    P = 
}

int main(){


    return 0;
}